diff --git a/src/postprocessing/mem_image.cpp b/src/postprocessing/mem_image.cpp
index 95d582a..b5791e3 100644
--- a/src/postprocessing/mem_image.cpp
+++ b/src/postprocessing/mem_image.cpp
@@ -225,12 +225,19 @@ int LibRaw::copy_mem_image(void *scan0, int stride, int bgr)
   ushort *ppm2;
   int c, row, col, soff, rstep, cstep;
 
-  soff = flip_index(0, 0);
-  cstep = flip_index(0, 1) - soff;
+  const int base = flip_index(0, 0);
+  cstep = flip_index(0, 1) - base;
   rstep = flip_index(1, 0) - flip_index(0, S.width);
-
-  for (row = 0; row < S.height; row++, soff += rstep)
+  // Closed form of the running index. cstep/rstep are constant, so row r
+  // starts where the serial loop would after r full rows.
+  const long long row_stride = (long long)cstep * S.width + rstep;
+
+#ifdef LIBRAW_USE_OPENMP
+#pragma omp parallel for private(col, ppm, ppm2, soff, c) schedule(static)
+#endif
+  for (row = 0; row < S.height; row++)
   {
+    soff = (int)((long long)base + row_stride * row);
     uchar *bufp = ((uchar *)scan0) + size_t(row) * size_t(stride);
     ppm2 = (ushort *)(ppm = bufp);
     // keep trivial decisions in the outer loop for speed
diff --git a/src/postprocessing/postprocessing_utils.cpp b/src/postprocessing/postprocessing_utils.cpp
index 9e1d527..6f5001e 100644
--- a/src/postprocessing/postprocessing_utils.cpp
+++ b/src/postprocessing/postprocessing_utils.cpp
@@ -78,11 +78,74 @@ void LibRaw::exp_bef(float shift, float smooth)
 
 void LibRaw::convert_to_rgb_loop(float out_cam[3][4])
 {
+  memset(libraw_internal_data.output_data.histogram, 0,
+         sizeof(int) * LIBRAW_HISTOGRAM_SIZE * 4);
+#ifdef LIBRAW_USE_OPENMP
+  // Per-pixel matrix and histogram. Thread-local counts are added at the end,
+  // so auto-bright sees the same histogram a serial pass would.
+  const int height = S.height;
+  const int width = S.width;
+  const int colors = imgdata.idata.colors;
+  const int raw_color = libraw_internal_data.internal_output_params.raw_color;
+#pragma omp parallel
+  {
+    int local[4][LIBRAW_HISTOGRAM_SIZE];
+    memset(local, 0, sizeof local);
+#pragma omp for schedule(static)
+    for (int row = 0; row < height; row++)
+    {
+      ushort *img = (ushort *)imgdata.image[row * width];
+      for (int col = 0; col < width; col++, img += 4)
+      {
+        if (raw_color)
+        {
+          for (int c = 0; c < colors; c++)
+            local[c][img[c] >> 3]++;
+        }
+        else if (colors == 3)
+        {
+          float out0 = out_cam[0][0] * img[0] + out_cam[0][1] * img[1] +
+                       out_cam[0][2] * img[2];
+          float out1 = out_cam[1][0] * img[0] + out_cam[1][1] * img[1] +
+                       out_cam[1][2] * img[2];
+          float out2 = out_cam[2][0] * img[0] + out_cam[2][1] * img[1] +
+                       out_cam[2][2] * img[2];
+          img[0] = CLIP((int)out0);
+          img[1] = CLIP((int)out1);
+          img[2] = CLIP((int)out2);
+          local[0][img[0] >> 3]++;
+          local[1][img[1] >> 3]++;
+          local[2][img[2] >> 3]++;
+        }
+        else if (colors == 4)
+        {
+          float out0 = out_cam[0][0] * img[0] + out_cam[0][1] * img[1] +
+                       out_cam[0][2] * img[2] + out_cam[0][3] * img[3];
+          float out1 = out_cam[1][0] * img[0] + out_cam[1][1] * img[1] +
+                       out_cam[1][2] * img[2] + out_cam[1][3] * img[3];
+          float out2 = out_cam[2][0] * img[0] + out_cam[2][1] * img[1] +
+                       out_cam[2][2] * img[2] + out_cam[2][3] * img[3];
+          img[0] = CLIP((int)out0);
+          img[1] = CLIP((int)out1);
+          img[2] = CLIP((int)out2);
+          local[0][img[0] >> 3]++;
+          local[1][img[1] >> 3]++;
+          local[2][img[2] >> 3]++;
+          local[3][img[3] >> 3]++;
+        }
+      }
+    }
+#pragma omp critical(mv_rgb_hist)
+    {
+      for (int c = 0; c < 4; c++)
+        for (int i = 0; i < LIBRAW_HISTOGRAM_SIZE; i++)
+          libraw_internal_data.output_data.histogram[c][i] += local[c][i];
+    }
+  }
+#else
   int row, col, c;
   float out[3];
   ushort *img;
-  memset(libraw_internal_data.output_data.histogram, 0,
-         sizeof(int) * LIBRAW_HISTOGRAM_SIZE * 4);
   if (libraw_internal_data.internal_output_params.raw_color)
   {
     for (img = imgdata.image[0], row = 0; row < S.height; row++)
@@ -139,20 +202,26 @@ void LibRaw::convert_to_rgb_loop(float out_cam[3][4])
       }
     }
   }
+#endif
 }
 
 void LibRaw::scale_colors_loop(float scale_mul[4])
 {
-  unsigned size = S.iheight * S.iwidth;
+  // MSVC OpenMP rejects an unsigned loop index. The image is far below 2^31
+  // pixels; the multiply itself is unchanged.
+  const int size = (int)(S.iheight * S.iwidth);
 
   if (C.cblack[4] && C.cblack[5])
   {
-    int val;
-    for (unsigned i = 0; i < size; i++)
+#ifdef LIBRAW_USE_OPENMP
+#pragma omp parallel for schedule(static)
+#endif
+    for (int i = 0; i < size; i++)
     {
-      for (unsigned c = 0; c < 4; c++)
+      for (int c = 0; c < 4; c++)
       {
-        if (!(val = imgdata.image[i][c])) continue;
+        int val = imgdata.image[i][c];
+        if (!val) continue;
         val -= C.cblack[6 + i / S.iwidth % C.cblack[4] * C.cblack[5] +
                         i % S.iwidth % C.cblack[5]];
         val -= C.cblack[c];
@@ -163,9 +232,12 @@ void LibRaw::scale_colors_loop(float scale_mul[4])
   }
   else if (C.cblack[0] || C.cblack[1] || C.cblack[2] || C.cblack[3])
   {
-    for (unsigned i = 0; i < size; i++)
+#ifdef LIBRAW_USE_OPENMP
+#pragma omp parallel for schedule(static)
+#endif
+    for (int i = 0; i < size; i++)
     {
-      for (unsigned c = 0; c < 4; c++)
+      for (int c = 0; c < 4; c++)
       {
         int val = imgdata.image[i][c];
         if (!val) continue;
@@ -177,9 +249,12 @@ void LibRaw::scale_colors_loop(float scale_mul[4])
   }
   else // BL is zero
   {
-    for (unsigned i = 0; i < size; i++)
+#ifdef LIBRAW_USE_OPENMP
+#pragma omp parallel for schedule(static)
+#endif
+    for (int i = 0; i < size; i++)
     {
-      for (unsigned c = 0; c < 4; c++)
+      for (int c = 0; c < 4; c++)
       {
         int val = imgdata.image[i][c];
         val = int(val * scale_mul[c]);
